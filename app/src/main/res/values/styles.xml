<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="My_Theme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/Mane</item>
        <item name="colorPrimaryDark">@color/ManeDark</item>
        <item name="colorAccent">@color/Accent</item>
        <item name="android:colorForeground">@color/Black</item>
        <item name="android:navigationBarColor">@color/Black</item>
        <item name="android:colorBackground">@color/Base</item>
        <item name="android:statusBarColor">@color/ManeDark</item>
    </style>

    <style name="myTheme" parent="android:Theme.Material.Light.NoActionBar" />

</resources>
